import React from 'react';
import TopBar from './TopBar';
import MainNav from './MainNav';

const Navbar = () => {
  return (
    <div>
      <TopBar />
      <MainNav />
    </div>
  );
};

export default Navbar;
